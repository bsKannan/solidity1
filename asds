assending or desseding
pragma solidity ^0.4.0;
contract name
{
    uint256[] a;
    function get(uint256 n) public view returns(uint256[])
    {
        for(uint256 i=0;i<=n;i++)
       {
                   a.push(i);
       }
      for(uint256 j=n;n>=j;j--)
        {
           a.push(j);
        }
         return (a);
 }
}
